{
  "title": "LLMApplicationsOutput",
  "description": "Schema to structure the LLM output with multiple applications, each having a name and specific text.",
  "type": "object",
  "properties": {
    "applications": {
      "type": "array",
      "description": "List of applications generated by the model.",
      "items": {
        "type": "object",
        "properties": {
          "application_name": {
            "type": "string",
            "description": "Name of the application."
          },
          "specific_text": {
            "type": "string",
            "description": "Specific text or content related to this application."
          }
        },
        "required": ["application_name", "specific_text"]
      }
    }
  },
  "required": ["applications"]
}
